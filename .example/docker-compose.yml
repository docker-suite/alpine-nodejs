version: '3.7'

services:

    node:
        image: dsuite/alpine-nodejs:lts
        command: ["node", "app.js"]
        ports:
            - 3000:3000
        working_dir: /app
        volumes:
            - ./app:/app:ro
